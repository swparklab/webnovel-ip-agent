node_modules/
.env
.env.local
.env.*.local
data/projects/
*.log
.DS_Store

# 비공개 자료 — secret/ 폴더의 모든 파일은 절대 커밋/업로드 금지
secret/
secret/**

# 특허 초안 — 출원 전 비공개 (커밋/업로드 금지)
patent.md

# Claude Code local settings
.claude/settings.local.json
